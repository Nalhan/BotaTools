local _, BOTA = ... 

local LDB = LibStub("LibDataBroker-1.1")
local LDBIcon = LDB and LibStub("LibDBIcon-1.0")

function BOTA:LDBInit() 
    if LDB then
        -- TODO: initialize databroker to make minimap icon
    end
end


